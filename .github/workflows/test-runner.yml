name: test-runner
on: workflow_dispatch

jobs:
  smoke:
    runs-on: [afaci-runner-01]
    steps:
      - run: docker run --rm alpine echo ok
      - run: git --version
      - run: echo "script ok" > script.sh && bash script.sh
